let spacialStation = {
    name: "Receptora",
    plataformLimit: 15,
    shape: "Esférica"
}

//Podremos também ter objetos de arrays, assim como arrays de objetos
//Logo, suas funções podem ser utilizadas em ambos, Exemplo:
console.log(spacialStation.name.toUpperCase());
console.log("---\n");

let spaceshipNames = ["Fênix", "Puller", "Golias"];
console.log(spaceshipNames[0].toUpperCase());
